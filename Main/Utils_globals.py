GRAMMAR_FILE_NAME = "grammar.txt"            # This contain the grammar for each block Index (starting from 1. First line is the number of blocks)
TESTSDUMP_FILE_NAME = "testsdump.txt"        # A dump containing data of type TestDataType for each test saved (from 1 to number of block indices
BLOCKS_OFFSETS = "blocksOffsets.txt"         # Line i specifies blockIndex i. Content of line i => real block offset for that line


# Variant where entire data is stored as a binary file
SINGLE_MODEL_FILENAME = "model.bin"

# sub folder to write results
